App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(
            CurrentUser,
            User()
        );
        Set(
            MasterDateFormat,
            "dd.mm.yyyy"
        );
        Set(v_popup, 1);
        Set(customers,IUBH_KUNDEN_LESEN.Run());
        Set(SAPDeliveries, RKN_GET_DELIVERY_LIST.Run(DD_shippingpoint.SelectedText.Value,lb_loadingdate.Text))

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

